import Link from 'next/link';
import React from 'react';

const LoginPage = () => {
    return (
        <div>
            This is login page

            <Link>Login</Link>
        </div>
    );
};

export default LoginPage;